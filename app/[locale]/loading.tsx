import { LoadingComp } from "@/components/LoadingComp";
import React from "react";

// show up when the router will change
const LoadingGlobal = () => {
  return <LoadingComp />;
};

export default LoadingGlobal;
